version: 2

jobs:
  build:
    docker:
      - image: circleci/python:2.7.15-jessie-browsers
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: make deps
      - run:
          name: Run Tests
          command: make coverage
