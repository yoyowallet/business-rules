version: 2

jobs:
  build:
    working_directory: ~/business-rules
    docker:
      - image: circleci/python:2.7.15-jessie-browsers
    steps:
      - run:
          name: Install Dependencies
          command: make deps
      - run:
          name: Run Tests
          command: make coverage
